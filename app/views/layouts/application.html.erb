<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Photography Site" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <header class="bg-white shadow-md fixed top-0 left-0 right-0 z-10">
      <nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
          <%# 1. Site Logo/Brand %>
          <div class="flex-shrink-0">
            <%= link_to "Schramm Shots Photography", root_path, class: "font-bold text-xl text-blue-600" %>
          </div>

          <%# 2. Navigation Links %>
          <div class="flex items-center space-x-6">
            <%# Public Links %>
            <%= link_to "Home", root_path, class: "text-gray-600 hover:text-blue-600 transition-colors" %>
            <%= link_to "Galleries", galleries_path, class: "text-gray-600 hover:text-blue-600 transition-colors" %>

            <%# --- Smart Links (admin) --- %>
            <% if admin_signed_in? %>
              <%# Link to the admin dashboard %>
              <%= link_to "Admin Panel", admin_root_path, class: "text-green-600 hover:text-green-800" %>

              <%# Devise helper for the "Sign Out" link %>
              <%= link_to "Sign Out", 
              destroy_admin_session_path,
              data: { turbo_method: :delete },
              class: "text-red-600 hover:text-red-800" 
              %>

            <% else %>
              <%# Sign In link%>
              <%= link_to "Sign In", new_admin_session_path, class: "text-gray-600 hover:text-blue-600" %>
              <% end %>
          </div>
        </div>
      </nav>
    </header>
    <main class="pt-16">
      <%= yield %>
    </main>
  </body>
</html>
